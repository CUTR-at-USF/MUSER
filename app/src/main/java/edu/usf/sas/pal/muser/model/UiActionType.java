package edu.usf.sas.pal.muser.model;

public enum UiActionType {
         PLAY,
         PAUSE,
         SKIP,
         REPEAT,
         SEEK,
         CREATE_PLAYLIST,
         SELECT_CATEGORY,
         SELECT_ARTIST
}
